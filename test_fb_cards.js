const admin = require('firebase-admin');
const serviceAccount = require('./google-services (11).json'); // Wait, is this the admin SDK key?
// Actually I don't have the admin key here. google-services.json is for Android.
