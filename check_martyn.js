const admin = require("firebase-admin");
admin.initializeApp({ projectId: "gatekipa-bbd1c" }); // Need proper init, actually we can just point to production or use functions/utils/firebase if it uses admin.
